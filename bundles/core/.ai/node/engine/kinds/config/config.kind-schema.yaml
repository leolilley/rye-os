# ryeos:signed:2026-09-05T00:45:06Z:adf9b1f84583b7ba639f9c2044ef504ba6e17b1ad7aa85cf258bfcd8ec7b3081:/XfLhWMdVArxA7hO2vNEghQFVa0oJXgN9QWoOmAMWHoGMaARsCgK5QanS0wq2i6nik+EQOcnawZe2hdh5CPUBA==:741a8bc609b398aaec0685e5aefb682faf5129a66bd192f888d23bb642c18eea
category: "engine/kinds/config"
version: "1.1.0"
location:
  directory: config
effective_trust:
  include_references: false
resolution: []
# Config items remain non-executable. This contract only permits an installed,
# signed config selected by another runtime to pin exact locator-free content.
content:
  external_content:
    realization_derived: effective_external_realizations
    allowed_roots: []
    allowed_mount_roots: [project]
    max_declarations: 8
    large_content:
      max_total_bytes: 4294967296
formats:
  - extensions: [".yaml", ".yml"]
    parser: parser:ryeos/core/yaml/yaml
    signature:
      prefix: "#"
# Each config consumer enforces its own schema — config kind is an
# escape hatch for per-domain configs (execution.yaml, etc.).
# Identity composer — this kind has no multi-document composition (no extends chain, no inheritance). Items are loaded as-is.
composer: handler:ryeos/core/identity
composed_value_contract:
  root_type: mapping
  required: {}
metadata:
  rules:
    name:
      from: filename
      required: true
      match: filename
    version:
      from: path
      key: version
    description:
      from: path
      key: description
    category:
      from: path
      key: category
      required: true
      match: path
