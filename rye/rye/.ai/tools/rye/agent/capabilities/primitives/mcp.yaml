name: mcp
version: "1.0.0"
description: "External MCP capabilities (template - projects add their own)"

# This file is a TEMPLATE. Projects add specific MCP capabilities via:
# .ai/tools/agent/capabilities/<mcp-name>.yaml
#
# MCP capability naming: rye.mcp.<mcp_name>.<action>
# Example: rye.mcp.campaign-kiwi.send_email
#
# MCP name validation: ^[a-z0-9][a-z0-9-]{0,62}$
# Action validation: ^[a-z0-9][a-z0-9._-]{0,128}$

permissions: []

hierarchy: {}
